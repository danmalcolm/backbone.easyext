CHILDVIEWS
----------

What happens when attaching for second time? Need to prevent selectors from matching on elements within child views

Attach modes, e.g. attach, append

Improve sequence config, e.g. sequence: this.collection or sequence: modelArray sequence: "nameOfParentViewModelAttribute"
